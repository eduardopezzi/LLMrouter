"""LLMrouter — Auto-Router Proxy/Gateway for LLM models.

A proxy/gateway that dynamically routes requests to the best LLM model
based on prompt complexity, cost, and model capabilities. Includes a
self-evaluation module powered by a local LLM (Ollama).
"""

__version__ = "0.1.0"
